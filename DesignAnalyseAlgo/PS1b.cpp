#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> sacks{2, 2, 2};
   int n = sacks.size;
   while(n==3 && sacks[n-1] = n)
   {
       sacks.push_back(n);

   }
}
